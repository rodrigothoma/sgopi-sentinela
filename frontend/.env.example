# Deixe vazio (ou nem crie o .env) em desenvolvimento: o Vite faz proxy de /v1 e do
# WebSocket para http://localhost:8000 (ver vite.config.ts), sem depender de CORS.
# Preencha só quando o frontend for servido sem proxy na frente do backend.
#VITE_API_BASE_URL=http://localhost:8000
